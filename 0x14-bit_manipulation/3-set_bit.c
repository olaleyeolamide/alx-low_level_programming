#include <stdio.h>
#include "main.h"

/**
 * set_bit - set value of bit to 1
 * @n: number to evaluate
 * @index: given index
 * Return: set value
 */

int set_bit(unsigned long int *n, unsigned int index)
{
	int n;
	int set_val;
	int index;

	if (index < 64)
		set_val = 1;

	return (set_val);

}

